numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3

# Optional but recommended. Supplies TRUE non-penalty xG per player, which is
# otherwise approximated from the FPL bootstrap (which folds penalties into xG
# and does not report penalties taken). Materially sharpens attacking rates.
understatapi>=0.5
# Needed only by fm_priors.load_fm_export() for Football Manager .html exports.
lxml>=4.9
